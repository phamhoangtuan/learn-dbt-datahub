name: 'medallion_pipeline'
version: '1.0.0'
config-version: 2

profile: 'medallion_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  medallion_pipeline:
    bronze:
      +materialized: table
    silver:
      +materialized: table
    gold:
      +materialized: table
